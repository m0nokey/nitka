# NaiveProxy backhaul

Status: planned; not selectable by deployment state.

Implement the same backhaul contract as `ssh_tun`: isolated credentials,
stable internal endpoint, healthcheck, updater ordering, and rollback.
